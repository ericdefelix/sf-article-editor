export function HTMLMarkupRecorder() {
    // let messageArr = [...localStorageArray];
    // const gmtErrotDate = new Date();

    // ihLint.forEach(({ message }) => {
    //   const isRepeated =
    //     typeof localStorageArray.find(i => i.message === message) ===
    //       'object' ||
    //     typeof messageArr.find(i => i.message === message) === 'object';

    //   const newItem = {
    //     // type,
    //     date: gmtErrotDate.toLocaleString(),
    //     message: message,
    //     markup: ih
    //   };

    //   if (!isRepeated) {
    //     messageArr = [...messageArr, newItem];
    //   } else {
    //     messageArr = [...messageArr];
    //   }
    // });

  // const localStorageArray =
  //   JSON.parse(localStorage.getItem('SFCRX_DebugLog')) || [];

  // localStorage.setItem('SFCRX_DebugLog', JSON.stringify(messageArr));
}